<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NewChatApp" parent="Theme.Material3.DayNight.NoActionBar">

        <item name="android:statusBarColor">@color/mainn</item>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.NewChatApp" parent="Base.Theme.NewChatApp" />

    <style name="Dialogue">
        <item name="android:windowBackground">@drawable/empty_back</item>
        <item name="windowNoTitle">true</item>
        <item name="windowMinWidthMajor">80%</item>
        <item name="windowActionBar">@color/black</item>
    </style>




</resources>